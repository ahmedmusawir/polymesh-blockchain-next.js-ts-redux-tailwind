import SingleTokenContent from "@/components/page-view/studio/SingleTokenContent";
import { Inter } from "next/font/google";

const inter = Inter({ subsets: ["latin"] });

export default function Dashboard() {
  return <SingleTokenContent />;
}
